name: messenger_service
description: A new Flutter project.
version: 0.0.2

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  rxdart: any
  logger: any
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed: ^2.5.2
  build_runner: ^2.4.11

flutter:
